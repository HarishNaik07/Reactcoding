import React from 'react'

function Mycomponent() {
  return (
    <div>
        <h1>I am dynamically loaded
            <p>
            Use the map() function to render lists.Always provide a unique key to each list item.
            Use filter or conditional logic for selective rendering.Manage dynamic lists using React state (useState).
            Let me know if you need further examples or explanations! 
            </p>
        </h1>
    </div>
  )
}

export default Mycomponent